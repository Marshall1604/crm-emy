'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { Search } from 'lucide-react';
const workspace=[['Dashboard','/dashboard','▦'],['Clients','/clients','♙'],['Businesses','/businesses','▣'],['Tax Returns','/tax-returns','▤'],['Fees','/fees','$']] as const;
const manage=[['Team','/team','♚'],['Settings','/settings','⚙']] as const;
export function CrmShell({children}:{children:React.ReactNode}){const pathname=usePathname();const active=(href:string)=>pathname===href||pathname.startsWith(`${href}/`);return <div className="route-shell"><aside className="route-sidebar"><Link className="route-brand" href="/dashboard"><span>C<i>✓</i></span><b>CRM <em>EMY</em></b></Link><nav><p>WORKSPACE</p>{workspace.map(([label,href,icon])=><Link key={href} href={href} className={active(href)?'active':''} aria-current={active(href)?'page':undefined}><span>{icon}</span>{label}</Link>)}</nav><nav className="route-manage"><p>MANAGE</p>{manage.map(([label,href,icon])=><Link key={href} href={href} className={active(href)?'active':''} aria-current={active(href)?'page':undefined}><span>{icon}</span>{label}</Link>)}</nav><div className="route-user"><span>AT</span><div><b>Amy Tran</b><small>Administrator</small></div><button type="button" disabled aria-label="Account menu">•••</button></div></aside><section className="route-content"><header className="route-topbar"><label><Search size={15}/><input placeholder="Search clients, returns, EIN..."/><kbd>⌘ K</kbd></label><div><button type="button" disabled aria-label="Notifications">♢<i/></button><span>AT</span></div></header>{children}</section></div>}
